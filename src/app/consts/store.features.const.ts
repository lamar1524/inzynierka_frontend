import { IStoreFeatures } from '../interfaces';

export const STORE_FEATURES: IStoreFeatures = {
  auth: 'auth',
  main: 'main',
  posts: 'posts',
  groups: 'groups',
  core: 'core',
  profile: 'profile',
  chat: 'chat',
};
